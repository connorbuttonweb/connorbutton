{
  "name": "asset-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Local tooling to minify main.css, bundle the dashboard JS, and self-host a subsetted Font Awesome icon set. Manually rerun after editing sources, not CI, not deployed.",
  "scripts": {
    "build:css": "node scripts/build-css.js",
    "build:js": "node scripts/build-js.js",
    "build:icons": "node scripts/build-icons.js",
    "build": "npm run build:css && npm run build:js && npm run build:icons"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "6.4.0",
    "clean-css": "^5.3.3",
    "fontawesome-subset": "^4.6.0",
    "terser": "^5.49.2"
  }
}
